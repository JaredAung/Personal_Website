/** Vertical gap before each major section (after the first block on the page). */
export const contentSectionStack = "mt-12 sm:mt-14";

/** Card-style container so sections read as separate panels from body text. */
export const contentSectionSurface =
  "rounded-2xl border-2 border-slate-400 bg-white/80 p-6 shadow-sm sm:p-8 dark:border-slate-500 dark:bg-slate-900/55";

/** Section title — larger than body so hierarchy is obvious. */
export const contentSectionTitle =
  "text-xl font-semibold tracking-tight text-slate-900 dark:text-slate-100";

/** Space between section title and its content. */
export const contentSectionBody = "mt-6";

/** Vertical gap between nested blocks inside one section card. */
export const contentNestedStack = "space-y-8 sm:space-y-10";

/** Subsection panel inside a section card (e.g. individual project). */
export const contentSubsection =
  "rounded-xl border-2 border-slate-300 bg-slate-50/90 p-5 sm:p-6 dark:border-slate-600 dark:bg-slate-800/50";
